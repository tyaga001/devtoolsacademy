datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma.ts-client-js"
}

model Post {
  slug      String   @id
  title     String?
  views     Int      @default(0)
}